{
  "name": "mongo",
  "version": "1.0.0",
  "description": "to use the mongo shell, the mongo deamon should be running in the background",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6.3.0"
  }
}
